const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');
const root=path.join(__dirname,'..'),main=fs.readFileSync(path.join(root,'src/main.cjs'),'utf8'),renderer=fs.readFileSync(path.join(root,'src/renderer.js'),'utf8'),css=fs.readFileSync(path.join(root,'src/styles.css'),'utf8');
test('the Tracked list refreshes airing status and new-episode counts from live metadata instead of the stale track-time snapshot',()=>{assert.match(main,/const details=\(await Promise\.all\(trackedItems\.filter\(x=>x\.type!=='movie'\)\.map\(x=>metadata\(x\.type,x\.id\)\.catch/);assert.match(main,/mediaDb\.trackedUpdates\(details\)/);assert.match(main,/airingById=new Map\(details\.map\(d=>\[String\(d\.id\),airingStatus\(d\)\]\)\)/);assert.match(main,/newEpisodeCount:newEpisodesById\.get\(String\(item\.id\)\)\|\|0,airing:airingById\.get\(String\(item\.id\)\)\|\|''/)});
test('tracked cards show an airing pill and a new-episode badge instead of only the generic TRACKED label',()=>{assert.match(renderer,/item\.airing==='airing'\?'<span class="airing-pill airing-live">/);assert.match(renderer,/item\.airing==='upcoming'\?'<span class="airing-pill airing-upcoming">/);assert.match(renderer,/item\.newEpisodeCount\?`\$\{item\.newEpisodeCount\} NEW`:'TRACKED'/)});
test('the title detail header shows an airing status pill computed from the shared airingStatus helper',()=>{assert.match(renderer,/window\.mediaHubShared\.airingStatus\(meta\)/);assert.match(renderer,/airing-pill airing-ended">Ended</);assert.match(css,/\.airing-pill\{[^}]*display:inline-flex/);assert.match(css,/\.card-art \.airing-pill\{[^}]*position:absolute/);assert.match(css,/\.airing-live\{[^}]*color:#79f8b7/)});
