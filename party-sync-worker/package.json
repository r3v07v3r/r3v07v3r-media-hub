{
  "name": "r3-party-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Relay server for R3 Media Center's Watch Party 'relay' mode — deploy this to Cloudflare Workers so hosting/joining a party works over the internet regardless of either side's router/NAT (see the main app's src/main/media-hub/watchParty.ts for the client this implements the protocol for).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260731.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.118.0"
  }
}
